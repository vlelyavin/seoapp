DATABASE_URL="file:./dev.db"
AUTH_SECRET="change-me-to-random-secret-in-production"
AUTH_URL="http://localhost:3000"

# Google OAuth
# AUTH_GOOGLE_ID=""
# AUTH_GOOGLE_SECRET=""

# FastAPI backend
FASTAPI_URL="http://127.0.0.1:8000"
NEXT_PUBLIC_FASTAPI_URL="http://127.0.0.1:8000"

# Admin (optional — grants admin role, receives cron alerts)
# ADMIN_EMAIL="vlelyavin@gmail.com"

# Cron job auth (required in production)
# CRON_SECRET=""

# OAuth token encryption — generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# TOKEN_ENCRYPTION_KEY=""

# SMTP (optional — enables transactional emails)
# SMTP_HOST=""
# SMTP_PORT="587"
# SMTP_USER=""
# SMTP_PASS=""
# EMAIL_FROM="noreply@seo.lvdev.co"
